# V3 真实模型页面测试清单

这份清单只包含当前页面可以真实完成的操作，用来检查 Coordinator 选人、派发审批、Harness 执行、文件交付和重新规划的实际效果。

本轮共执行 7 个场景、产生 7 条 Run。场景 02 和 03 连续使用同一个空间，因此只需要创建 6 个测试空间。

## 开始前

- 使用真实模型运行 Worker：`yarn worker`。
- 不要设置 `AGENT_WORKER_FAKE=1`。
- 确认用户已经配置可用的模型。
- 每个空间只添加清单指定的成员，不要额外添加成员。
- 创建空间后，在空间设置中选择清单指定的执行模式并保存。
- 除场景 03 外，不要提前上传文件或发送其他消息。

建议按顺序执行。每个场景结束后记录：

```text
场景编号：
空间 ID（浏览器地址中的 spaceId）：
开始时间：
最终状态：
实际派发给谁：
是否通过：
发现的问题：
```

## 场景 01：单成员直接分析

### 空间设置

- 空间名称：`V3-01-直接分析`
- 成员：前端
- 执行模式：自动执行
- id：969b8ca2-beb5-461c-9a85-96b540a2c512
### 发送内容

```text
请分析下面这个移动端首页的信息层级，并给出正好 3 条可以直接实施的改进建议。

页面结构：
1. 顶部显示用户头像、欢迎语和搜索按钮
2. 中间依次展示公告横幅、快捷功能入口和最近任务
3. 最近任务每项显示标题、负责人、状态和更新时间
4. 底部固定显示首页、空间、消息、我的四个导航入口
5. 当前主要问题是首屏信息较多，用户不容易判断应该先操作什么

本次只需要分析和建议，不需要联网，也不要创建或修改文件。
```

### 通过标准

- [ ] Coordinator 只派发 1 个任务给前端。
- [ ] 页面没有显示正在联网或联网完成。
- [ ] 空间没有新增文件。
- [ ] 最终结果包含正好 3 条具体建议。
- [ ] Run 状态为已完成，目标覆盖为 100%。

## 场景 02：从零创建单页应用

### 空间设置

- 空间名称：`V3-02-页面创建和修改`
- 成员：前端
- 执行模式：自动执行

### 发送内容

```text
请在空间工作区创建 index.html，完成一个可以直接预览的移动端待办应用。

要求：
1. 可以新增待办事项
2. 可以切换完成和未完成状态
3. 可以删除待办事项
4. 显示全部、未完成、已完成三个筛选入口
5. 适配 375px 宽度，底部操作区域不能遮挡内容
6. CSS 和 JavaScript 全部写在 index.html 内，不使用 CDN、外部图片或其他网络资源

最终必须交付真实文件，不要只描述代码。
```

### 通过标准

- [ ] Coordinator 只创建 1 个端到端前端任务，没有拆成设计、开发、检查多个任务。
- [ ] 工作区生成且只生成一个主要页面文件 `index.html`。
- [ ] 文件检查通过，任务详情显示文件变化。
- [ ] 点击预览可以看到页面，不是空白页。
- [ ] 新增、完成、删除和筛选操作可用。
- [ ] Run 状态为已完成，目标覆盖为 100%。

## 场景 03：继续修改已有页面

这个场景不要新建空间，继续使用场景 02 的 `V3-02-页面创建和修改`。确认场景 02 已经完成后再发送下一条消息。

### 发送内容

```text
请修改工作区现有的 index.html，不要创建新的 HTML 页面。

修改要求：
1. 底部筛选栏固定在视口底部
2. 为 iPhone 安全区域增加适配
3. 列表底部保留足够间距，最后一条待办不能被筛选栏遮挡
4. 保留现有的新增、完成、删除和筛选功能
5. 保持现有视觉风格，不要整体重写与本次修改无关的内容

完成后直接交付修改后的文件。
```

### 通过标准

- [ ] Coordinator 仍只派发 1 个前端任务。
- [ ] 成员读取并修改现有 `index.html`，没有创建第二个 HTML 文件。
- [ ] 原有四项交互功能仍然可用。
- [ ] 375px 宽度下最后一条内容不被底部栏遮挡。
- [ ] Run 状态为已完成，目标覆盖为 100%。

## 场景 04：产品与前端连续协作

### 空间设置

- 空间名称：`V3-04-多成员协作`
- 成员：产品、前端
- 执行模式：自动执行

### 发送内容

```text
请设计并实现一个移动端订单筛选页面，最终交付可直接预览的 index.html。

产品成员先明确：
1. 支持哪些订单状态
2. 日期和金额筛选规则
3. 已选择条件如何展示和清除
4. 无结果状态及验收标准

前端成员再根据产品结论实现页面。页面使用内置模拟数据，CSS 和 JavaScript 写在 index.html 内，不使用外部资源。产品只负责规则和验收标准，不修改文件；前端负责完整页面交付。
```

### 通过标准

- [ ] Coordinator 先给产品安排 advisor 任务，再给前端安排 executor 任务。
- [ ] 产品任务没有修改工作区文件。
- [ ] 前端任务能够看到并采用产品结论。
- [ ] 没有给两位成员重复安排同一项实现工作。
- [ ] 最终 `index.html` 可以预览，筛选规则与产品结论一致。
- [ ] Run 状态为已完成，目标覆盖为 100%。

## 场景 05：派发前确认

### 空间设置

- 空间名称：`V3-05-派发确认`
- 成员：前端
- 执行模式：派发前确认

### 发送内容

```text
请创建一个可直接预览的移动端商品管理页面 index.html。

页面需要包含商品搜索、上架状态筛选、商品列表和新增商品表单。使用内置模拟数据，CSS 和 JavaScript 写在 index.html 内，不使用外部资源。
```

### 页面操作

1. 等待页面出现 Coordinator 的派发提案。
2. 先不要点击按钮，观察成员是否已经开始执行。
3. 确认提案的成员、执行说明和验收标准合理。
4. 点击“确认并开始”。

### 通过标准

- [ ] 确认前任务保持待确认状态，没有任务开始或文件生成事件。
- [ ] 提案明确展示交给谁、为什么以及准备做什么。
- [ ] 点击确认后前端才开始执行。
- [ ] 最终生成可预览的 `index.html`。
- [ ] Run 状态为已完成，目标覆盖为 100%。

## 场景 06：退回后重新规划

### 空间设置

- 空间名称：`V3-06-退回重规划`
- 成员：产品、前端
- 执行模式：派发前确认

### 发送内容

```text
请为客服团队创建一个可直接预览的移动端工单列表页面 index.html，需要支持状态筛选和查看工单详情。
```

### 页面操作

1. 等待第一次派发提案，不要确认执行。
2. 点击“拒绝”。
3. 根据第一次提案填写一条明确且方向相反的调整要求：
   - 如果第一次交给前端，填写：`请先交给产品明确工单状态、优先级和列表验收规则，再由前端实现。`
   - 如果第一次交给产品，填写：`需求已经明确，不需要产品分析，请直接交给前端完成页面。`
4. 点击“退回并重新规划”。
5. 等待新的派发提案，确认它符合刚才的调整要求后，再点击“确认并开始”。

### 通过标准

- [ ] 退回后 Run 没有结束或变成已取消。
- [ ] 原派发提案显示为已取消，成员没有执行原提案。
- [ ] Coordinator 的新提案能够体现退回原因。
- [ ] 新提案仍然需要用户确认，没有绕过审批。
- [ ] 确认新提案后任务可以继续并最终完成。
- [ ] 质量报告中的“退回重规划”至少增加 1 次。

## 场景 07：审批时调整派发内容

### 空间设置

- 空间名称：`V3-07-调整派发`
- 成员：产品、前端
- 执行模式：派发前确认

### 发送内容

```text
请创建一个移动端库存盘点页面 index.html，供仓库人员记录商品的系统库存和实际库存。
```

### 页面操作

1. 等待 Coordinator 给出派发提案。
2. 点击“调整派发”。
3. 将执行成员改为“前端”。
4. 将任务标题改为：`实现移动端库存盘点页面`。
5. 将执行说明改为：

```text
创建 index.html。页面展示商品名称、SKU、系统库存和实际库存输入框；自动计算盘盈或盘亏；支持只看有差异的商品；使用内置模拟数据；所有 CSS 和 JavaScript 内联，不使用外部资源。
```

6. 将验收标准改为：

```text
index.html 可以直接预览；可以修改实际库存；差异数量会自动更新；只看差异筛选可用；375px 宽度下没有横向滚动。
```

7. 保存调整并确认开始。

### 通过标准

- [ ] 调整后的成员、标题、说明和验收标准被保存。
- [ ] 实际执行任务使用调整后的内容，而不是原提案内容。
- [ ] 只执行 1 个前端任务，没有额外新增重复任务。
- [ ] 最终页面满足调整后的验收标准。
- [ ] Run 状态为已完成，目标覆盖为 100%。

## 完成后生成报告

所有场景结束后运行：

```bash
yarn v3:report --days 7
```

本轮预期结果：

- 共新增 7 条 V3 Run，全部进入终态。
- 7 条 Run 均为 `COMPLETED`。
- 目标覆盖率为 100%。
- 场景 06 会产生至少 1 次退回重规划，因此一次完成率不要求为 100%。
- 完成消息投递异常为 0。
- 不存在长期停留在 `PLANNING`、`RUNNING`、`REVIEWING` 或 `SUMMARIZING` 的 Run。

发现不符合预期的场景时，不要立刻重复执行。先保留空间，记录空间 ID 和开始时间，用于查询对应 Run、任务和事件数据。

## 离线回归基线

01–07 全部通过后，成功 Run 会以脱敏 JSON 保存在 `tests/fixtures/v3/`。日常代码修改无需重新手工输入七个场景，运行：

```bash
yarn v3:check
```

该命令使用临时 SQLite 重放已验收流程，并运行项目自动化测试、TypeScript、Worker 语法和 Diff 格式检查，不读取或修改当前业务数据库，也不调用真实模型。

只有完成一轮新的真实模型验收并确认结果正确后，才使用 `yarn v3:fixtures` 更新夹具。
